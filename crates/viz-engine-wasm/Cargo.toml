[package]
name = "viz-engine-wasm"
version = "0.1.0"
description = "WASM bindings for viz-engine browser computation kernels."
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
js-sys = "0.3"
serde.workspace = true
serde-wasm-bindgen = "0.6"
viz-engine-core = { path = "../viz-engine-core" }
wasm-bindgen = "0.2"
